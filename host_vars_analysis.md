# Ansible host_vars 問題分析と改善策

## 問題パターン1: YAML構文エラー（インデントミス）

**原因**: YAMLのインデントルールに従わず、不正なインデントを使用。
**影響**: Ansible実行時にパースエラー発生、プレイブック停止。
**対策**: YAML標準の2スペースインデントを使用、一貫性を保つ。

## 問題パターン2: 変数型ミス

**原因**: VLAN IDを文字列として定義（例: "20"）したが、Ansibleで数値として扱う必要がある。
**影響**: 設定適用時に型不一致エラー、予期せぬ動作。
**対策**: 数値型はクォートなし、数値型はクォート付きで統一。

## 問題パターン3: 構造的一貫性の欠如

**原因**: インターフェース設定でvlanキーがトップレベルに誤配置。
**影響**: 変数参照時にKeyError、設定漏れ。
**対策**: 変数構造を標準化、テンプレートを使用。

## 問題パターン4: 重複変数定義

**原因**: 同じインターフェースに複数のvlan設定。
**影響**: Ansibleが最後の定義を使用、予期せぬ上書き。
**対策**: 変数定義前に重複チェックを実施。

## 問題パターン5: 変数名の一貫性の欠如

**原因**: インターフェース名にGi0/1とGigabitEthernet0/1の混在。
**影響**: 参照エラー、設定漏れ。
**対策**: 命名規則を定義、lintツール使用。

## ベストプラクティス

- 変数名はsnake_case統一
- 構造は辞書形式で標準化
- YAML validator使用
- host_varsはホスト固有変数として使用
- テンプレート化で再利用性向上

## Ansible Playbooks の問題パターン

### 問題パターン6: Ansibleモジュール構文エラー

**原因**: ios_configモジュールのlinesパラメータがタスク内で正しくインデントされていない。
**影響**: 構文エラーによりプレイブック実行失敗、設定変更不可。
**対策**: Ansibleドキュメントを確認し、正しいパラメータ構造を使用。

### 問題パターン7: 変数参照エラー

**原因**: host_vars変数がプレイブック内で正しく参照されていない（例: {{ variable }}の構文ミス）。
**影響**: 変数未定義エラー、設定値が適用されない。
**対策**: host_vars変数をwith_itemsやloopで適切に参照、debugモジュールで変数内容確認。

### 問題パターン8: タスク順序ミス

**原因**: インターフェース設定前にVLAN作成を忘れる、shutdown前に設定変更。
**影響**: 設定適用順序によりネットワーク中断や設定失敗。
**対策**: ベストプラクティスに従い、論理インターフェース→物理インターフェース→有効化の順序。

### 問題パターン9: モジュール名タイポ

**原因**: ios_configをios_configと誤記、または古いモジュール名使用。
**影響**: モジュールが見つからないエラー、プレイブック停止。
**対策**: Ansibleバージョンに合った正しいモジュール名を使用、スペルチェック。

### 問題パターン10: インデント構造破壊

**原因**: copyタスクのloopパラメータがタスクレベルから外れるインデントミス。
**影響**: YAMLパースエラー、タスク実行不可。
**対策**: YAML構造を常に確認、ansible-lintツール使用。

## 新規host_vars作成チェックリスト

- [ ] YAML構文チェック（python -c "import yaml; yaml.safe_load(open('file.yml'))"）
- [ ] インデントの一貫性確認（2スペース）
- [ ] 変数型の一貫性（id: 数値, name: 文字列）
- [ ] キー名の統一（interfaces, vlans）
- [ ] 重複変数チェック
- [ ] Ansible dry-runテスト
- [ ] バージョン管理コミット

## 新規playbook作成チェックリスト

- [ ] Ansible構文チェック（ansible-playbook --syntax-check）
- [ ] モジュールパラメータの正しさ確認
- [ ] 変数参照の正確性確認（{{ }}構文）
- [ ] タスク順序の論理的妥当性確認
- [ ] host_varsとの連携確認
- [ ] dry-run実行テスト
- [ ] lintツール実行（ansible-lint）
